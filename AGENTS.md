# YunXi-Agent-Platform 项目级 Agent 规则

## 项目性质与代码构建原则

本项目是用于准备 Java 后端 / AI Agent 开发工程师面试的复杂学习项目，业务设计需要贴近真实运营商客服、政企客户经理、工单、商机、套餐政策与知识治理场景。所有架构设计、代码实现、测试与文档都应服务于两个目标：一是形成可运行、可演示的后端能力闭环；二是让实现过程具有明确学习价值，方便面试时讲清楚为什么这样设计、如何落地、如何治理复杂度。

### 最少代码最佳实现

- 默认采用满足需求的最少代码实现，避免为了“显得高级”而堆叠抽象、模式或中间层。
- RuoYi-Cloud-Plus、Spring Boot、Spring Cloud Alibaba、Sa-Token、MyBatis-Plus、Redis、Nacos 等框架已有能力应优先复用，不重复造基础设施。
- 能通过框架配置、已有工具类、通用组件、拦截器、注解和现有模块边界解决的问题，不新增自定义框架。
- 新增第三方能力、starter、SDK、BOM 或中间件客户端前，必须先检查原框架是否已经提供等价能力或依赖；能复用已有能力则优先复用，确实缺失时再做最小化引入，并说明引入原因、版本来源和影响范围。
- 新增抽象必须有明确收益：降低重复、隔离变化、表达领域概念、承载扩展点或提升可测试性。
- 首版实现优先跑通闭环，复杂能力通过清晰接口预留，不提前实现未验证需求。

### 贴合框架代码风格

- 新增代码优先遵循 RuoYi-Cloud-Plus 现有分层、命名、包结构、异常处理、响应结构、配置方式和日志风格。
- Controller、Service、Mapper、Domain、DTO/VO/BO、Dubbo API、配置类等结构应与现有模块保持一致。
- 不引入与当前框架风格冲突的目录结构、返回模型、权限模型或全局异常体系。
- 业务模块可以独立表达 YunXi Agent/RAG/Tool/Memory 边界，但不要破坏现有网关、认证、租户、权限、配置中心和服务注册机制。
- 开发中新增必要配置时，必须先查看框架已有配置处理方式，并贴合现有配置源、命名层级、覆盖规则和环境隔离方式；本框架作为优秀设计应优先学习和复用。
- 代码注释优先解释业务约束、设计取舍、关键边界和非显然逻辑，不写机械性注释。

### 学习价值要求

- 架构设计必须能讲清楚真实业务问题、模块职责、数据流、失败场景、扩展点和治理手段。
- 代码实现应体现后端核心能力训练：事务边界、幂等、异步任务、限流熔断、权限隔离、审计日志、可观测 Trace、测试覆盖和性能退化策略。
- Agent 相关能力不能只停留在调用大模型 API，应体现 Workflow、Tool Calling、RAG、Memory、Evaluation、Prompt 版本和 Bad Case 闭环。
- 运营商业务复杂度应通过 mock 表、样例数据和清晰领域模型体现，重点模拟客户、套餐、工单、投诉、商机、沟通纪要、知识库和质检规则。
- C 端 H5 只作为接口演示和流量模拟入口，不把复杂度投入到移动端、真实录音或前端工程。

### Spring AI 与大模型调用原则

- 本项目的 Agent 开发主线必须优先采用 Spring AI，不以自封装通用 LLM HTTP Client 作为主要学习目标。
- 对 OpenAI Compatible、Gemini、DeepSeek、Qwen 等模型资源，优先通过 Spring AI 的 ChatClient、Advisor、Tool Calling、VectorStore、Memory 等能力接入。
- 大模型 `base-url`、`model`、`api-key` 等配置应贴合框架既有配置体系处理，如按场景进入 Nacos 配置中心，不硬编码在源码、脚本或测试样例中。
- 当前默认大模型资源走 OpenAI Compatible 网关：`base-url` 使用 `https://new.lemonapi.site`，模型使用 `[L]gemini-2.5-flash`，API Key 由 Nacos 或环境变量提供。
- Python 或其他脚本形式的大模型调用样例仅作为资源连通性验证材料，不应直接进入正式 Java 后端架构。
- 不重复造通用模型调用轮子；只有在供应商协议兼容性不足、框架能力确实缺失或需要解释底层原理时，才允许局部使用 WebClient 作为兜底适配。
- 若 Maven 镜像缺少 Spring AI 依赖，可修正本机 Maven settings，例如 `D:\APP\Apache-maven-3.9.11-bin\conf\aliyun-settings.xml`，优先保证 Maven Central 或可用镜像能解析 Spring AI artifacts。
- 面试学习重点应放在 Agent 工程化认知与设计：Agent Runtime、Workflow、Skill/Tool Registry、MCP Client/Server、RAG、Memory、Trace、Evaluation、Prompt 版本、Bad Case 治理和权限审计。
- 与 Claude Code、Codex、OpenClaw、Hermes 等 Agent 系统相关的学习，应沉淀为架构思路、工具治理、任务编排、上下文压缩、执行安全和可观测设计，而不是停留在模型接口调用。

### 设计与实现取舍

- 任何新增模块先明确：它解决什么业务问题、依赖哪些现有能力、暴露什么接口、失败后如何恢复、如何验证。
- 对共享能力保持克制：只有被两个以上明确场景复用，或属于 Agent/RAG/Tool/Memory 核心边界时，才上升为公共模块。
- 云犀业务数据建议新增独立数据库和表，尽量不改动原 RuoYi 框架库与既有业务表，通过服务边界、配置和接口完成解耦。
- 对写操作工具必须考虑权限、参数校验、幂等、人工确认或审计留痕。
- 对高并发训练场景优先选择后端可控入口，例如批量模拟通话记录导入、Agent SSE 并发、RAG 检索并发、异步摘要任务堆积。
- 文档、代码和测试应保持一致；当产品定位、模块边界或关键流程变化时，同步更新设计文档。

## 工具脚本约定

- `script/tools` 下的脚本优先保证代码可读性，避免把复杂逻辑堆在一段命令或一个大函数里。
- 脚本开头应使用注释说明用途、输入、输出和典型运行方式。
- 脚本头部的常用命令必须带中文解释，说明每条命令会做什么、是否会写入外部服务。
- Python 脚本应拆分为职责清晰的函数，优先使用标准库；确需第三方依赖时，应说明安装方式和原因。
- 本项目运行 Python 工具脚本前，默认使用 `conda activate base`。
- 对外部服务有写入行为的脚本，应提供 `dry-run` 或等价的只读预览模式。
